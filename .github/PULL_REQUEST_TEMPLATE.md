<!-- Thanks! Accepted changes are applied upstream and land with the next sync
     (see CONTRIBUTING.md) — this PR will be closed with a link to that commit. -->

**What changes and why:**

**How it was verified** (`go test -race ./...`, a real run with `--test`, …):

- [ ] `gofmt -l .` is clean and `go vet ./...` passes
- [ ] Tests added or updated
- [ ] `COMPATIBILITY.md` updated if a wkhtmltopdf flag changed class
- [ ] No API key or job output committed
