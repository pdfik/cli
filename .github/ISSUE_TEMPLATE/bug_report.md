---
name: Bug report
about: Something the CLI does wrong
labels: bug
---

<!-- Remove your API key from anything you paste: command lines, environment
     dumps, logs. Keys look like sk_live_… — if you pasted one, rotate it at
     https://pdfik.net/dashboard/api-keys. -->

**Command** (with the key removed):

```
pdfik …
```

**What happened** (stderr, exit code — `echo $?`):

```
```

**What you expected:**

**Version and platform:** `pdfik version`, OS/arch, install method (release
binary, `go install`, Docker).

**Job id** (printed as `job <id> queued`), if any — it lets support look at
the server side without your key.
